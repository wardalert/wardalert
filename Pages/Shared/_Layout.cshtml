<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
@* 
    <!-- Correcting the Bootstrap CSS link -->
    <link rel="stylesheet" asp-page="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" asp-page="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" asp-page="~/wardalert.styles.css" asp-append-version="true" />
 *@
    <title>Welcome to Lalitpur</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
        }

        /* Navigation Bar */
        .navbar {
            position: fixed;
            width: 100%;
            background-color: #3B5998;
            padding: 2px;
            display: flex;
            align-items: center;
            z-index: 1000;
        }

      


        .logo {
            width: 120px;
            height: 100px;
            margin-right: 5rem;
        }

        .nav-links {
            display: flex;
            gap: 7rem;
            padding-top: 30px;
        }

            .nav-links a {
                color: white;
                text-decoration: none;
                font-size: 1.2rem;
                text-transform: uppercase;
            }

       

        /* Main Content */
         .content-wrapper {
            margin-left: 4px;
            margin-right:4px;
        }  

        .main-content {
            width: auto;
            height: auto;
            margin: 0 auto;
            padding: 1rem;
            justify-content: space-between;
            display: flex;
        }

        .welcome-image {
            width: 850px;
            height: 500px;
            display: block;
            justify-content: center;
            align-items: center;
            margin-left: 300px;
            border: 15px solid black;
            margin-top: 10px;
            position: absolute;
            top: 20%;
        }

        .haku {
            width: 85px;
             margin-left:-12px;
            position: fixed;
            top: 104px;
        }

        .welcome-text {
            position: absolute;
            top: 55%;
            left:28%;
            color: rgb(181, 24, 24);
            
            font-size: 3rem;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            
            white-space: nowrap;
            font-family: Georgia, 'Times New Roman', Times, serif;
        }

        .ward {
            color: #d4d3d3;
        
            font-size: 1.5rem;
            text-transform: uppercase;
            margin-left:auto;
            font-style: bold;
            padding-right:10px;
        }

        .text {
            margin-left: 180px;
            margin-right: 180px;
            margin-top: 650px;
            position: relative;
            font-size: medium;
            word-spacing: 1.5px;
            letter-spacing: 0.3px;
        }

        .info-section {
            text-align: center;
        }

        #options {
            color: rgb(248, 245, 245);
            background-color: #807979;
        }
        /* Dropdown Menu Styling */
        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
            z-index: 1;
        }

            .dropdown-content a {
                color: black;
                padding: 12px 16px;
                text-decoration: none;
                display: block;
            }

              
     
        .dropdown:hover .dropdown-content {
            display: block;
        }

        .dropdown:hover .dropbtn {
            background-color: #3B5998;
        }
    </style>
</head>
<body>

    <div class="content-wrapper">
      @*   <img src="./images/12.png" class="haku"> *@
        <nav class="navbar">
            <img src="./images/ki.png" alt="Lalitpur Logo" class="logo">
            <div class="nav-links">
                <a asp-page="Index">Home</a>
                <a asp-page="/Notice">Notice</a>

                <!-- Dropdown Menu without Bootstrap -->
                <div class="dropdown">
                    <a asp-page="#" class="dropbtn">Training</a>
                    <div class="dropdown-content">
                        <a asp-page="/Upcoming">Upcoming</a>
                        <a asp-page="/Ongoing">Ongoing</a>
                        <a asp-page="/Expired">Completed</a>
                    </div>
                </div>
                
                   
               

            </div>
            @{
                var isLoggedIn = Context.Session.GetString("Login") == "true";
            }
            @if (isLoggedIn)
            {
                <p style="color:#3B5998;">hellohellooooo</p>
                <div class="nav-links">
                    <a asp-page="/Dashboard">dashboard</a>
                    <a asp-page="/Userlist">userlist</a>
                </div>

            }
            <a class="ward">ward 17</a>
          
        </nav>

     

       @*  <!-- Include Bootstrap Bundle (with Popper.js) -->
        <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

        <!-- Optionally, include site.js for custom scripts -->
        <script src="~/js/site.js" asp-append-version="true"></script> *@ 
    </div>
    @* Check if user is logged in *@
  

    
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

   
</body>

</html>

